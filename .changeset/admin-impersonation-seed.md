---
'@scilent-one/auth': minor
'@scilent-one/db': minor
---

Enable Better Auth admin plugin (roles + user impersonation) and seed mock users with basic profiles for local development. Adds admin schema fields, `db:seed`, and Admin → Users impersonate/stop-impersonate UI with middleware and layout guards.
