---
'@scilent-one/scilent-ui': minor
---

Use `next/image` for artwork surfaces (`Artwork`, `AlbumArtwork`, `ArtistCard`, `ArtistHeader`) so the Next app can optimize remote CDN images. `next` is an optional peer dependency; Storybook aliases `next/image` to a native `<img>` mock.
